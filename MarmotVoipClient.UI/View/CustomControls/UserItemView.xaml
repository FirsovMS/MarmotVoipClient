<UserControl x:Class="MarmotVoipClient.UI.View.CustomControls.UserItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 mc:Ignorable="d">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto"/>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>
		<Border CornerRadius="50" Background="{Binding Color}">
			<Label Content="{Binding Initials, TargetNullValue=Text}" 
				   HorizontalAlignment="Center" VerticalAlignment="Center"/>
		</Border>
		<StackPanel Orientation="Vertical" Grid.Column="1">
			<Label Content="{Binding ProfileName, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
			<Label Content="{Binding Message, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
		</StackPanel>
	</Grid>
</UserControl>
