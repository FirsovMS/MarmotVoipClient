<UserControl x:Class="MarmotVoipClient.UI.View.FooterControlsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MarmotVoipClient.UI.View"
             mc:Ignorable="d" >
    <UserControl.Resources>
        <BitmapImage x:Key="numpad" UriSource="pack://application:,,,/Resources/Images/calls.png"/>
        <BitmapImage x:Key="history" UriSource="pack://aplication:,,,/Resources/Images/presenceAway.png"/>
    </UserControl.Resources>
    <StackPanel Orientation="Horizontal">
        <Button x:Name="btnNumpad"
                Width="50">
            <ContentControl>
                <Image Source="{StaticResource numpad}"/>
            </ContentControl>
        </Button>
        <Button x:Name="btnHistory"
                Width="50">
            <ContentControl>
                <Image Source="{StaticResource history}"/>
            </ContentControl>
        </Button>
    </StackPanel>
</UserControl>
